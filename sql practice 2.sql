use gap22l;
show tables;


UPDATE EMPLOYEE
SET DEMPARTMENT = HR;

SELECT *
FROM Books
WHERE AuthorId = 1;

SELECT EMPLOYEE, HR
FROM DEPARTMENT
JOIN HR ON HR = EMPLOYEE.DEPT_ID
JOIN EMPLOYEE AS MANAGER ON DEPARTMENT.MANAGER_ID = MANAGER.EMP_ID
WHERE DEPARTMENT.DEPT_NAME = 'IT';





